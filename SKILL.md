---
name: tidy-files
description: 整理、归纳、收拢文件的专用流程——把散落在桌面、下载夹、文稿夹、iCloud 云盘、家目录等处的文件，按「一个大文件夹 → 一项目一夹 / 按流程阶段 / 按文件类型 → 小任务或零散件兜底」的嵌套结构归到一起，只移动、改名和新建，绝不删除或覆盖，最后写 README 索引并逐夹核对。只要用户在说"整理一下文件 / 桌面 / 下载夹"、"把这些东西归类 / 归纳到一个文件夹"、"清理桌面"、"按项目分开"、"截图视频统一放好"、"整理云盘"、"把 agent 做的东西 / 论文材料 / 散件收拢"，哪怕没出现"整理"两个字，只要意图是把一批文件挪进有结构的文件夹里，就用这个 skill。反例：删除重复文件、清理磁盘空间、卸载软件、压缩打包，不是这个 skill。
---

# tidy-files：整理文件

## 这个 skill 要解决什么

用户手上的文件通常是几个月里被各种工具（Claude / DeepSeek / Codex、浏览器下载、微信、截图、iCloud）随手落下的：同一个项目散在桌面、下载夹和 `~/Documents/Codex/<日期>/<会话>` 三处，论文稿子有八九个版本躺在不同地方，截图铺满桌面。用户想要的不是"清理"，而是**能一眼看全、以后能找到**：一个大文件夹进去，每个项目 / 每个类型一个子夹，子夹里还能再分，外加一个索引说明每样东西是什么、从哪来。

整个过程最重要的约束是**可逆和不丢东西**：只做 `mkdir` / `mv`（含改名），不 `rm`，不覆盖同名文件。用户看不到你敲的命令，只看结果；一个被覆盖的旧稿子就是不可挽回的事故，而一个多出来的 `重复副本/` 文件夹只是一点点不整齐。

## 流程

按下面五步走。每一步都不长，但顺序不要乱：**先盘点、再设计、给用户看一眼、然后才动手**。

### 第 0 步：先把范围和基准说清楚

从用户的话里抓三件事，抓不到就问：

1. **范围**：只桌面？整台电脑（桌面 + 下载 + 文稿 + 家目录 + iCloud）？还是某个云盘？用户经常先说"整理桌面"，做完又说"我说的是整个电脑"——所以一开始就问一句"只桌面还是整台电脑"能省一轮返工。
2. **主题**：整理什么——agent 产出、论文材料、零散件、还是全部？不同主题用不同结构（见第 2 步）。
3. **基准位置**：同一个项目在多处出现时，以哪里为准、往哪里并。默认以桌面（或用户指定的大文件夹）为基准，其他位置的同类项往基准里并。

### 第 1 步：盘点

- 用 `ls -la` 把范围内每个目录看一遍，子目录也看（Codex 会话夹、下载夹里数字编号的报告夹这类看名字猜不出内容的，一定要点进去看）。
- 名字看不出归属的文件，先看内容再归类：图片直接 Read 看一眼（可能是录用通知单、豆瓣截图、随手拍），CSV / md 读前几行。别按文件名猜。
- 一边看一边记三类东西：
  - **写死路径、不能挪的**：被 launchd / cron / systemd 引用的目录（去 `~/Library/LaunchAgents/*.plist` 里对一下 `ProgramArguments` 和 `WorkingDirectory`）、被别的项目 `.env` 或脚本硬编码引用的目录、Python venv（挪了就失效）、应用托管的工作区（Codex App 按路径记会话夹、iCloud 同步夹被别的程序当落盘点）。
  - **同名撞车**：不同位置的同名文件（往往是同一份下载了两次，也可能是不同版本）。
  - **不属于本次主题的**：另一个人的资料、系统残留（`desktop.ini`、`$RECYCLE.BIN`、`.DS_Store`）、游戏存档、App 资料库（照片图库、剪映）。这些默认不动，写进"没收进来的"清单里。
- 权限够不到的地方（比如外接盘 `/Volumes`）明说没检查，别装作看过了。

更多具体的坑见 [references/pitfalls.md](references/pitfalls.md)，动手前扫一遍。

### 第 2 步：设计结构，给用户看一眼

三种基本模式，按主题选（可以混用：一个大文件夹里某个子夹再按类型分）：

| 模式 | 适用 | 例子 |
|---|---|---|
| **按项目** | agent 产出、代码、多个独立事情 | `agent/` 下 `daily-report/`、`stock-tool/`、`Our/`……零散产出统一进 `小任务/` |
| **按流程阶段** | 论文、申报、一件事的全过程 | `毕业论文/` 下 `01-选题与开题/ 02-初稿与修改稿/ 03-查重报告/ 04-答辩/ 05-终稿/ 06-插图/ 07-代码与仿真/ 08-过程管理与手续/ 09-模板与参考/ 10-工具安装包/` |
| **按文件类型** | 无处可归的散件，区分度故意不高 | `零散文件/` 下 `截图/ 图片/ 视频/ 文档/ 安装包/ 其他/`，文档里再按明显的组分一层（`课程资料/ 准考证/ 考研资料/`） |

命名和结构上的几条经验：

- **大文件夹只放一层项目 / 类别**，项目内部再嵌套；用户要的是"进一个门就能看全"。
- 子夹名用用户的语言、按内容起名（`03-查重与AIGC检测报告` 好过 `reports`）；需要保持顺序的加两位数字前缀，Finder 里就是按流程排的。
- **能整夹改名搬入的，不要逐个文件搬**：`mv ~/Desktop/开题报告 ~/Desktop/毕业论文/01-选题与开题` 一步到位，桌面上不会留空壳。
- 一串 URL 编码或数字编号的名字（`%E5%85%B3...pdf`、`2026060808185641292977/`）改成可读的中文名，改名等于移动，允许。
- 同名撞车放 `重复副本/`（或 `其他版本（来自下载）/`），两份都留着，README 里说明哪份大小/日期不同。
- 来自别的位置并进来的会话夹，名字里带上来源和日期，方便回溯：`codex-2026-07-17-handoff-md-App图标概念/`。
- 空文件夹、空壳一般留在原地不动（用户可自己删），README 里点一句。

设计好后用一段简短的树状图给用户看，同时把**边界问题**一次问完（用选项、给推荐）：
- 明显是别人的 / 另一件事的材料要不要收？
- 云盘里的文件是搬到本地，还是就地整理（搬出云盘 = 手机上看不到了）？
- 安装包 / 工具目录算不算这次的东西？
- 应用托管的工作区（Codex 会话夹）是原地建索引，还是并进项目？

只在"读法不同会导致不同结构"时才问；能用常识定的（临时锁文件放哪、录用通知单归到"毕业手续"）自己定，写进 README。

### 第 3 步：执行——只 mkdir 和 mv

- 只用 `mkdir -p` 和 `mv`（改名也是 mv）。不用 `rm`、不用 `cp`（复制会留两份反而更乱）、不用 `mv -f` 往已存在的文件上盖。
- 目标已存在就换个目标（`重复副本/`），别覆盖。`scripts/tidy.sh` 里的 `mvs` 就是干这个的：目标存在则跳过并打印 `SKIP`，否则 `MOVED`。
- **分批、小命令、绝对路径、加引号**。中文名、空格、括号、`=`、`[` 都可能让一条长命令被权限系统拦下或解析出错；一次搬一组同类文件，失败了单个重试。
- 顺序：先整夹搬（改名搬入），再搬散件进去，最后处理撞名的。
- 跨位置搬要先想副作用：
  - 从 iCloud 搬到本地 = 云端不再保留，手机上看不到——要用户点头；用户要求"云盘的就在云盘里整理"时就在云盘内建同样的结构。
  - 移走 launchd / 脚本写死的目录会让定时任务静默失败：要么不动，要么在旧路径留一个替身链接 `ln -s 新路径 旧路径`，并在 README 里写清楚。
  - git 仓库整个搬走没问题（`.git` 跟着走）；往仓库里加子夹只是多几个未跟踪文件。
- 写死路径的项目就是不动。README 里加粗写"路径写死，不要再挪"。

### 第 4 步：写 README 索引

每个大文件夹根目录放一个 `README.md`，用户是不看命令行的，这个文件就是他们以后找东西的地图。固定用这个模板：

```markdown
# <大文件夹名>

<一句话：什么时候、从哪几个地方、按什么方式归拢的；只做了移动/改名/新建，没有删除。>

| 子文件夹 | 里面是什么 | 原来在哪 |
|---|---|---|
| `01-…/` | … | 桌面 `xxx/`（整个改名搬入）+ 下载夹 |

## 备注 / 没收进来的
- 可以删的临时文件（如 Word 的 `~$xxx` 锁文件）——没删，留给用户
- 按用户选择没收的东西
- 需要用户手动做的一步（例如 `ln -s …`）
- 没权限看的位置
```

大文件夹里的每一行都要能回答"这是什么、原来在哪"。搬过来的会话夹 / 报告夹如果名字仍然晦涩，就在这里解释。

### 第 5 步：核对并汇报

- `ls -R` 或逐夹 `ls` 把新结构过一遍，确认每个计划里的文件都到位、来源目录只剩下"故意不动"的东西。
- 汇报用一段树状图 + "需要你知道的几点"（副作用、留给用户手动做的事、没收进来的、剩下的空壳）。别把命令贴给用户看，除非是需要他们自己点一下运行的那一条。
- 如果某个操作因为权限或工具问题没做成，明说哪个没做成、给一条他们可以直接运行的命令，不要留在"下次再说"里。

## 一次典型的对话长什么样

用户："把我用 agent 做的东西都整理一下，一个大文件夹，每个项目一个小文件夹，小任务放一起，不要删文件。"

1. 问一句范围（只桌面还是整台电脑），说明会先盘点再给方案。
2. 盘点桌面、下载、文稿、`~/Documents/Codex`、家目录；查 LaunchAgents 找出写死路径的项目。
3. 给树状图：`agent/` 下一项目一夹 + `小任务/`；标出"不动"的项目；把边界问题（别人的资料、Codex 会话夹、云盘）用选项问一次。
4. 只 mkdir/mv 执行；撞名进 `重复副本/`；写死路径的留替身。
5. 写 `agent/README.md`；`ls` 核对；汇报树 + 注意事项。

## 附带资源

- [scripts/tidy.sh](scripts/tidy.sh)：`source` 之后可用的两个函数——`mvs <源> <目标目录>`（安全移入，目标存在则跳过）和 `mvr <源> <新路径>`（安全改名，目标存在则跳过）；以及 `inv <目录...>`（按扩展名统计散件，帮你快速看清一个目录里有什么）。
- [references/pitfalls.md](references/pitfalls.md)：踩过的坑清单——写死路径、应用托管工作区、iCloud、文件名里的特殊字符、系统残留文件等。动手前读一遍。
- [references/structures.md](references/structures.md)：三种结构模式的完整示例和 README 模板，设计阶段照着改。
